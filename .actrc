# act configuration file
# This file configures how act runs GitHub Actions locally

# Use medium-sized Docker images (good balance of speed and compatibility)
# GitHub Workflow matrix OS should match the list of target platforms below
-P ubuntu-latest=catthehacker/ubuntu:act-latest
-P macos-latest=catthehacker/ubuntu:act-latest
-P macos-14=catthehacker/ubuntu:act-latest

# Don't use real GitHub token by default (use --secret GITHUB_TOKEN=... if needed)
# -s GITHUB_TOKEN=

# Enable verbose output by default
# --verbose

# Container architecture
--container-architecture linux/amd64

# Reuse containers for faster subsequent runs
--reuse

# Bind working directory
--bind
